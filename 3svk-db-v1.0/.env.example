# 3SVK DB configuration
PORT=5000
DATA_FILE=./database.json
UPLOADS_DIR=./uploads
# If deploying behind a domain, set so file URLs are absolute (no trailing slash)
# PUBLIC_URL_BASE=https://api.3svkdb.com
